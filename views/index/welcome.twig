{% extends "frontend.twig" %}

{% block content %}
<div class="row">
	<div class="col-lg-5 col-md-5">
		<div class="jumbotron">
			<h1>Welcome to Horaro</h1>
			{% if not noRegister %}
			<p>This tool allows you to create schedules (specifically tailored towards live stream marathons).
			It's free (as in free beer) and available both as a hosted service and as an
			<a href="https://bitbucket.org/sgt-kabukiman/horaro" target="_blank">open-source application</a>.</p>
			{% endif %}
			<p>
				{% if noRegister %}
				<small><em>User registration is not available in this installation.</em></small>
				{% else %}
				<a href="/-/register" class="btn btn-primary btn-md" role="button"><i class="fa fa-user"></i> Create an Account</a>
				<small>or log-in with your existing account.</small>
				{% endif %}
			</p>
		</div>
	</div>

	<div class="col-lg-7 col-md-7">
		<h3>Upcoming Events (<a href="/-/calendar/{{ date()|date('Y/m') }}">Calendar</a>)</h3>

		{% if upcoming|length > 0 %}
		<ul class="h-eventlist">
			{% for row in upcoming %}
			<li class="h-jail">
				{% set start = row.schedules[0].start %}
				<a href="/{{ row.event.slug }}" class="event"><i class="fa fa-gamepad"></i> {{ row.event.name|shorten(45) }}</a>
				<time datetime="{{ start.format('Y-m-d\\TH:i:sP') }}" title="{{ start.format('l, F d, Y \\a\\t H:i') }} ({{ row.schedules[0].timezone }} time)" class="h-relative h-fuzzy text-muted">on {{ start.format('l, F d, Y') }}</time>
				({% for schedule in row.schedules %}<i class="fa fa-calendar"></i> <a href="/{{ row.event.slug }}/{{ schedule.slug }}">{{ schedule.name|shorten(45) }}</a>{% if not loop.last %}, {% endif %}{% endfor %})
			</li>
			{% endfor %}
		</ul>
		{% else %}
		<div class="alert alert-info">
			There are no upcoming events with schedules here, sorry.
		</div>
		{% endif %}

		<h3>Past Events</h3>

		{% if featured|length > 0 %}
		<ul class="h-eventlist">
			{% for event in featured %}
			<li class="h-jail"><a href="/{{ event.slug }}" class="event"><i class="fa fa-gamepad"></i> {{ event.name|shorten(45) }}</a></li>
			{% endfor %}
		</ul>
		{% endif %}
	</div>
</div>

<div class="row">
	<div class="col-lg-12 col-md-12">
		<h3>Some Features</h3>
	</div>
</div>

<div class="row">
	<div class="col-lg-3 col-md-3 col-sm-4 col-xs-6 text-center">
		<h4>Responsive</h4>
		<p><img src="{{ macros.asset('images/feature-responsive.png') }}" alt="Screenshot showing a schedule on a small screen" class="img-thumbnail"></p>
		<p><small>Schedules work well even on tablets and phones.</small></p>
	</div>

	<div class="col-lg-3 col-md-3 col-sm-4 col-xs-6 text-center">
		<h4>Always Up-to-Date</h4>
		<p><img src="{{ macros.asset('images/feature-up-to-date.png') }}" alt="Screenshot showing the current/next item in a schedule" class="img-thumbnail"></p>
		<p><small>See immediately what's on right now and what's next to come.</small></p>
	</div>

	<div class="col-lg-3 col-md-3 col-sm-4 col-xs-6 text-center">
		<h4>Open Data</h4>
		<p><img src="{{ macros.asset('images/feature-exports.png') }}" alt="Screenshot showing the JSON export of a schedule" class="img-thumbnail"></p>
		<p><small>Access all schedules as JSON(P), XML or CSV.</small></p>
	</div>

	<div class="col-lg-3 col-md-3 col-sm-4 col-xs-6 text-center">
		<h4>Flexible</h4>
		<p><img src="{{ macros.asset('images/feature-flexible.png') }}" alt="Screenshot showing the JSON export of a schedule" class="img-thumbnail"></p>
		<p><small>You can control the schedule columns as you see fit.</small></p>
	</div>

	<div class="col-lg-3 col-md-3 col-sm-4 col-xs-6 text-center">
		<h4>Keep Your Privacy</h4>
		<p><img src="{{ macros.asset('images/feature-privacy.png') }}" alt="" class="img-thumbnail"></p>
		<p><small>No Facebook, no Twitter, no Google+, no Adsense, no nothing.</small></p>
	</div>

	<div class="col-lg-3 col-md-3 col-sm-4 col-xs-6 text-center">
		<h4>Themes</h4>
		<p><img src="{{ macros.asset('images/feature-themes.png') }}" alt="Screenshot of a schedule using the Amelia theme" class="img-thumbnail"></p>
		<p><small>Select from a number of different themes for your schedules.</small></p>
	</div>

	<div class="col-lg-3 col-md-3 col-sm-4 col-xs-6 text-center">
		<h4>Private Schedules</h4>
		<p><img src="{{ macros.asset('images/feature-private.png') }}" alt="" class="img-thumbnail"></p>
		<p><small>Mark schedules as private and only share them with people you want to.</small></p>
	</div>

	<div class="col-lg-3 col-md-3 col-sm-4 col-xs-6 text-center">
		<h4>iCal Feeds</h4>
		<p><img src="{{ macros.asset('images/feature-ical.png') }}" alt="Screenshot showing the default Android calendar showing imported schedule items" class="img-thumbnail"></p>
		<p><small>Subscribe to a schedule to get all dates constantly updated in your personal calendar.</small></p>
	</div>
</div>
{% endblock %}
